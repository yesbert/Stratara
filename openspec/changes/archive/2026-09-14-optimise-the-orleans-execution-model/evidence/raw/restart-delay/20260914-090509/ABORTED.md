Aborted after the Default setting: the run sent "exit" to the host and waited for a reply the host never gives. The Default numbers stand — cold start ready 1.5 s, three restarts on the same endpoint ready in 1.3–1.4 s, first command applied 1.6 s each; the silo marks its older clone dead on start ("Detected older version of myself"). The ShortIAmAlive setting did not run. The run is reshaped: what waits after a restart is the reminder service, not the join, so the next run measures the time until a durable timer registered before the kill fires after the restart.
