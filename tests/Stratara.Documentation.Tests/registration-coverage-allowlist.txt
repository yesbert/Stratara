# Registrations deliberately absent from docs/reference/di-extensions-cheatsheet.md.
# One per line: <method name> — <reason>. An entry that no longer resolves to a
# registration fails DiCheatsheetCoverageTests, so this list cannot outlive its subjects.
AddNpsqlWriteDbContextFactory — [Obsolete] alias kept for the original misspelling; documenting it would advertise the typo. Removed in the next major version.
UseAuthorizationExceptionTo403 — [Obsolete], superseded by AddStrataraProblemDetails(); registering both makes this middleware answer first and the problem-details handler never see the exception. Removed in the next major version.
